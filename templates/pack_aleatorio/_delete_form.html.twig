<form method="post" action="{{ path('pack_aleatorio_delete', {'id': pack_aleatorio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pack_aleatorio.id) }}">
    <button class="btn">Delete</button>
</form>
