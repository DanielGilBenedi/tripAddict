<form method="post" action="{{ path('hotel_servicio_delete', {'id': hotel_servicio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hotel_servicio.id) }}">
    <button class="btn">Delete</button>
</form>
