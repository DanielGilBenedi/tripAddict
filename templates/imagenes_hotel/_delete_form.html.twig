<form method="post" action="{{ path('imagenes_hotel_delete', {'id': imagenes_hotel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ imagenes_hotel.id) }}">
    <button class="btn">Delete</button>
</form>
