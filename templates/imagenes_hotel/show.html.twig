{% extends 'base.html.twig' %}

{% block title %}ImagenesHotel{% endblock %}

{% block body %}
    <h1>ImagenesHotel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ imagenes_hotel.id }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ imagenes_hotel.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('imagenes_hotel_index') }}">back to list</a>

    <a href="{{ path('imagenes_hotel_edit', {'id': imagenes_hotel.id}) }}">edit</a>

    {{ include('imagenes_hotel/_delete_form.html.twig') }}
{% endblock %}
