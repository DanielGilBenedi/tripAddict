<form method="post" action="{{ path('estancia_pack_delete', {'id': estancia_pack.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ estancia_pack.id) }}">
    <button class="btn">Delete</button>
</form>
