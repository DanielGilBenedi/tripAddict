{% extends 'base.html.twig' %}

{% block title %}EstanciaPack{% endblock %}

{% block body %}
    <h1>EstanciaPack</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ estancia_pack.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('estancia_pack_index') }}">back to list</a>

    <a href="{{ path('estancia_pack_edit', {'id': estancia_pack.id}) }}">edit</a>

    {{ include('estancia_pack/_delete_form.html.twig') }}
{% endblock %}
