{% extends 'base.html.twig' %}

{% block title %}Pack{% endblock %}

{% block body %}
    <h1>Pack</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pack.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ pack.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ pack.descripcion }}</td>
            </tr>
            <tr>
                <th>Precio</th>
                <td>{{ pack.precio }}</td>
            </tr>
            <tr>
                <th>ImagenPortada</th>
                <td>{{ pack.imagenPortada }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pack_index') }}">back to list</a>

    <a href="{{ path('pack_edit', {'id': pack.id}) }}">edit</a>

    {{ include('pack/_delete_form.html.twig') }}
{% endblock %}
